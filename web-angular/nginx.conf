# Sirve el build de Angular bajo /nuevo/ (el gateway le reenvía esas rutas)
server {
    listen 80;
    root /usr/share/nginx/html;

    location /nuevo/ {
        try_files $uri $uri/ /nuevo/index.html;
    }
}
